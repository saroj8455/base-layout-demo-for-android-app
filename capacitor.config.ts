import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devapihub.base',
  appName: 'Base App',
  webDir: 'dist/base-layout-demo/browser',
};

export default config;
